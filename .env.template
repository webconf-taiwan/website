APP_TITLE=Webconf
APP_DESC=Webconf
APP_DEFAULT_LANG=zh-TW

APP_URL=http://localhost:3000
APP_BASE_URL=/

# API 位置（SSR 可用 APP_API，瀏覽器端走 public.APP_API）
APP_API=http://localhost:3000/api

# 後台 API（Hono + D1，獨立 Worker，見 backend/）。
# 本機開發對應 backend 那邊 `yarn dev` 起的 wrangler dev 預設埠。
ADMIN_API_URL=http://localhost:8787

WEB_SEARCH=NO

# 是否保留 console.log / console.info（YES 保留，其他移除）
SHOW_CONSOLE_LOG=YES
