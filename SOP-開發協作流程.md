# SOP：功能開發協作流程

> 這份是從「管理者後台功能擴充」這次的實際合作過程歸納出來的通用流程，之後遇到類似規模（牽涉後端架構、多角色協作、執行階段可能無人在場監督）的任務，可以直接照這份走。核心精神：**事前由你主導規劃與拍板，執行階段交給 Claude 自主完成，全程用一份持續更新的 Markdown 規格文件當單一事實來源。**

## 目錄

1. 適用情境
2. 角色分工原則
3. Phase 0：規格收斂
4. Phase 1：分工與執行前準備
5. Phase 2：執行
6. Phase 3：驗收
7. 誠實回報原則
8. 下次直接照抄的檢查清單

---

## 1. 適用情境

- 規模大到需要拆解成多個步驟、多個角色參與。
- 涉及既有系統的架構調整（不是單純加一個功能，而是可能動到共用的底層寫法）。
- 執行階段你可能不在場（出門、開會），需要 Claude 能自主判斷「現在能做什麼、什麼要停下來等你」。

小改動不需要走完整套流程，直接做就好；這套 SOP 是給「值得先花時間對齊規格」的任務用的。

## 2. 角色分工原則

- 常見角色：前端、後端、PM、QA、設計師。人力不夠時這些角色可以都由你＋Claude 扮演，重點是**職責要分清楚**，不是真的要找五個人。
- **PM 的職責要明確包含「驗收守門」**：不是只排分工、對齊進度，也是最終驗收前的第一關，避免「差不多但沒人簽核」的狀態。
- **設計師（或任何需要先看現況的角色）適合當一次性的 kickoff 稽核**，不需要全程參與後續開發——先走查現況、產出具體清單（哪裡不好用、卡在哪），當作後續定稿規格的輸入，而不是憑空討論。
- **QA 不是一次性角色**，要一直待到最終驗收前；每次有修正就要重測一輪，範圍越大越需要全程盯著。
- 如果後端／某個角色的工作量本身變大，考慮往下拆兩層：
  1. **基礎建設**（架構重構、共用工具建置）——必須先做完，會卡住後面所有人。
  2. 地基完成後，才拆成**可以平行進行的多條功能線**（彼此不碰同一批檔案的話，才真的能平行，不然只是換個順序做）。

## 3. Phase 0：規格收斂

目標：把一句話的需求，變成一份雙方都同意、沒有模糊地帶的規格文件。

1. **建一份 Markdown 規格文件**，從第一次討論就開始寫，隨對話持續更新，不要等「想清楚了」才動筆。
2. 逐項討論時，**每一條結論都要明確標記狀態**：「定案」還是「待確認」，不要留模糊的句子（例如「大概這樣」「先這樣好了」）。
3. Claude 主動指出規格裡的缺口、給建議，但**要清楚區分「這是我的建議」跟「這是已經定案的決定」**，不能自己偷偷拍板。
4. **不確定的外部事實一律先查證，不用記憶猜**：套件是否還在維護、官方推薦做法是不是換了、平台有沒有限制。訓練時記住的做法可能已經過時（這次 `@cloudflare/vitest-pool-workers` 換成 `@cloudflare/vitest-plugin` 就是活生生的例子——查證抓到才改對，不查證就會照著過時的方式做下去）。
5. 討論到一定篇幅後，**規格文件本身會開始有矛盾、重複、過時的內容**——這是正常的，不是失敗。要主動排一次「最終收斂」：
   - 拿掉已經被推翻但還留著的舊敘述（不然讀起來像還沒定案）。
   - 把散落在各節裡的「待確認」項目全部收進同一張總表，不要有漏網之魚。
   - 加目錄、加索引表（例如這次的「API 總覽」），讓讀者不用拼湊多個章節才看得懂全貌。
6. **退出條件**：待確認事項總表全部確認完，才進入分派執行階段。

## 4. Phase 1：分工與執行前準備

1. 依「待辦分工」清單排優先序：**地基優先、有依賴關係的項目排前面**（例如：沒有新增功能就沒有資料可以測查看/編輯/刪除，Migration 一定要排在用到新欄位的 API 前面）。
2. **測試帳號規範**：
   - 各角色用自己的測試帳號，測完自行清除；使用者本人正在用的真實帳號絕對不能動（不能刪、不能改、也不能拿來測試危險操作）。
   - 正式 CRUD 功能做出來之前，需要測試帳號的話，由 Claude 直接對**本機開發環境**手動塞一個一次性帳號，用完即刪。
   - 全程只在本機／隔離環境操作測試帳號，不要碰 remote／production，就算操作失誤也不會波及真實資料。
3. **如果執行階段你會離開**：
   - 確認開發伺服器都正常在跑。
   - 確認裝置能撐住整個工作階段不會中途睡著／斷線（這次的具體案例：查 `pmset` 發現系統睡眠計時器只有 1 分鐘，用 `caffeinate -i` 擋住；查 `system_profiler` 確認電池健康度足夠撐完預估時間）。
   - 讓 Claude 知道：遇到規格裡還沒定案、真的需要你決定的地方，**要停下來記錄清楚、跳過那一塊，不能用猜的硬做**。

## 5. Phase 2：執行

1. **對外部技術決策二次查證**：安裝套件、設定框架前，先確認官方目前推薦的做法，不要完全依賴記憶——函式庫的 API 可能已經改版。
2. **動到「不能改變行為」的既有程式，一定要有 before/after 的迴歸驗證**，而且要用客觀的比對方式（例如語意比對 JSON 內容），不能只憑肉眼看過去覺得像。這次 Menu 選單重構、忙中打錯測試資料就是靠這個機制當場抓到並修正的。
3. **危險或敏感的規則（例如「最後一位 Super Admin 不能被刪除」），要用隔離的測試環境驗證，不要拿共用的本機資料庫或正式環境的真實資料操作**——共用資料庫裡本來就會有其他測試帳號干擾判斷，容易誤判規則有沒有生效。
4. **型別檢查、測試都要真的跑過、看到通過才算數**，不能寫完就假設是對的；每個小階段做完就驗證一次，不要累積到最後才一次驗證。
5. 過程中如果操作出錯（例如改壞了某筆資料、服務意外當機），**立刻處理、立刻用客觀方式確認已經修好**，不要放著先做別的。

## 6. Phase 3：驗收

- 規格文件本身兼作驗收清單，驗收時直接對照「待辦分工」逐項打勾，不用另外生一份驗收文件。
- 搭配專案裡既有的自動化工具（例如跑起來實際操作驗證的 skill、多寬度截圖檢查 RWD 的 skill）。
- **QA 除錯迴圈**：測試 → 回報問題 → 對應角色修正 → 重新驗證 → 交還 QA 重測，這個迴圈會反覆進行，直到跑一輪都沒有新問題，才進入最終簽核。QA 沒過，不進最終驗收。
- 真正拍板永遠是你本人。

## 7. 誠實回報原則

- 重大里程碑（例如一個階段做完）要主動回報進度，不要悶頭做完全部才一次講。
- **過程中的失誤要老實揭露，即使是自己造成的**：說清楚哪裡出錯、怎麼發現的、怎麼修的、怎麼確認修好了。不要略過不提，也不要等對方問才承認。
- 給時程或範圍評估時，**誠實區分「有把握的部分」跟「真正無法預先知道的變數」**，不要為了給一個乾脆的數字就假裝所有變數都在掌握中。

## 8. 下次直接照抄的檢查清單

開始一個新任務時，依序問自己（或請 Claude 主動確認）：

- [ ] 有沒有一份持續更新的 Markdown 規格文件？
- [ ] 這次的角色怎麼分？「基礎建設」跟「功能線」有沒有分開？
- [ ] 待確認事項總表收斂完了嗎？還有沒有殘留的模糊地帶？
- [ ] 待辦分工的順序有沒有照依賴關係排（地基優先）？
- [ ] 測試帳號怎麼建、在哪裡建、誰負責清？
- [ ] 如果執行階段沒人在場：環境撐得住嗎？卡住規格問題時該怎麼處理，講清楚了嗎？
- [ ] 動到既有行為的地方，有沒有 before/after 的客觀驗證機制？
- [ ] 危險規則有沒有在隔離環境測過，而不是在共用/正式資料上測？
- [ ] 每個階段做完，有沒有真的跑過驗證（型別檢查、測試），不是憑感覺？
- [ ] 驗收機制講清楚了嗎？QA 除錯迴圈跑到什麼程度才算過？
